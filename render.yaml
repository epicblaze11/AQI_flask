services:
  - type: web
    name: AQI_flask
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app"
    buildFilter:
      paths:
        - .
    envVars:
      - key: RENDER_PYTHON_VERSION
        value: 3.10.4
